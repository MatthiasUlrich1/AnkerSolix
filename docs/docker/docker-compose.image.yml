# Example: custom image with optional Python 3.12 for ioBroker.anker-solix
# Not required from adapter 0.10.87 (stock buanet Python 3.11 is accepted).
# See ../docker-buanet.md
services:
  iobroker:
    container_name: iobroker
    build:
      context: .
      dockerfile: Dockerfile
    image: iobroker-anker-solix:latest
    hostname: iobroker
    restart: always
    ports:
      - "8081:8081"
    volumes:
      - iobrokerdata:/opt/iobroker
    environment:
      - TZ=Europe/Berlin

volumes:
  iobrokerdata:
